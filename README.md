# FastAPI Микросервисы: ToDo List + URL Shortener

![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-009688?style=for-the-badge&logo=fastapi&logoColor=white)
![SQLite](https://img.shields.io/badge/Database-SQLite-003B57?style=for-the-badge&logo=sqlite&logoColor=white)
![Docker](https://img.shields.io/badge/Container-Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white)
![Python](https://img.shields.io/badge/Python-3.11+-3776AB?style=for-the-badge&logo=python&logoColor=white)

Два независимых микросервиса на **FastAPI** с хранением данных в **SQLite** и полной контейнеризацией через **Docker**.

- **ToDo Service** — полноценный CRUD для списка задач  
- **URL Shortener** — создание коротких ссылок, редирект и статистика  

Оба сервиса имеют автоматическую **Swagger-документацию** по адресу `/docs`.
## 📝 Задание

### Требования к сервисам

#### Общие требования:
- Каждый сервис — отдельное FastAPI-приложение
- Сервисы должны иметь автоматическую документацию по адресу `/docs`
- Данные должны храниться в SQLite. Файл базы данных должен находиться в директории, которая будет подключена как том при запуске контейнера

#### ToDo-сервис:
**Эндпоинты:**
- `POST /items` — создание задачи (title, description?, completed=false)
- `GET /items` — получение списка всех задач
- `GET /items/{item_id}` — получение задачи по ID
- `PUT /items/{item_id}` — обновление задачи по ID
- `DELETE /items/{item_id}` — удаление задачи

**Требования:**
- Все операции должны работать с базой SQLite
- Перед запуском автоматически создаётся таблица, если она не существует

#### Сервис сокращения URL:
**Эндпоинты:**
- `POST /shorten` — принимает полный URL (JSON: `{"url":"…"}`) и возвращает короткую ссылку
- `GET /{short_id}` — перенаправляет на полный URL, если он существует
- `GET /stats/{short_id}` — возвращает файл JSON с информацией о полном URL

**Требования:**
- Данные о сокращённых ссылках (short_id → full_url) хранятся в SQLite
- При запуске автоматически создаётся таблица

#### Требования к Docker:
- Для каждого сервиса нужно написать Dockerfile
- Dockerfile должен:
  - Установить зависимости (`pip install -r requirements.txt`)
  - Скопировать код сервиса
  - Объявить `VOLUME /app/data` для хранения данных
  - Запускать Uvicorn-приложение на порту 80

#### Запуск сервисов:
```bash
docker run -d -p 8000:80 -v todo_data:/app/data <имя_образа_todo>
docker run -d -p 8001:80 -v shorturl_data:/app/data <имя_образа_shorturl>
```

Где `todo_data` и `shorturl_data` — заранее созданные именованные тома.

#### Проверка работоспособности:
**ToDo-сервис:**
- `POST /items` для создания задачи
- `GET /items` для получения списка задач
- Должен сохранять задачи после перезапуска контейнера благодаря тому, что база в томе

**Сокращение ссылок:**
- `POST /shorten` для создания короткой ссылки
- `GET /{short_id}` для редиректа
- `GET /stats/{short_id}` для просмотра данных о ссылке
- Данные о ссылках должны сохраняться после перезапуска контейнера

#### Загрузка кода на GitHub:
- Создать публичный репозиторий на GitHub
- Загрузить исходный код обоих сервисов, каждый в своей директории
- Добавить `README.md` с описанием, как запускать сервисы локально и через Docker

#### Публикация образов на Docker Hub:
- Создать учётную запись на Docker Hub
- Собрать образы локально
- Запушить образы на Docker Hub

---

## ✨ Особенности реализации

- Полностью рабочие REST API
- Автоматическое создание таблиц в SQLite при запуске
- Данные сохраняются между перезапусками контейнеров (именованные тома)
- Интерактивная документация Swagger UI
- Готовые Docker-образы на Docker Hub

## 📁 Структура проекта
```
fastapi-microservices/
├── todo_app/ # ToDo микросервис
│   ├── main.py # Основное приложение FastAPI
│   ├── database.py # Модели и подключение к SQLite
│   ├── requirements.txt # Зависимости Python
│   └── Dockerfile # Docker конфигурация
├── shorturl_app/ # URL Shortener микросервис
│   ├── main.py # Основное приложение FastAPI
│   ├── database.py # Модели и подключение к SQLite
│   ├── requirements.txt # Зависимости Python
│   └── Dockerfile # Docker конфигурация
├── docker-compose.yml # Оркестрация контейнеров
└── README.md # Эта документация
```

---

## 🚀 Быстрый старт (рекомендуемый способ)

```bash
# 1. Клонируем репозиторий
git clone https://github.com/BSergey2000/fastapi-microservices.git
cd fastapi-microservices
```
```bash
# 2. Запускаем оба сервиса
docker-compose up -d
# Готово! Сервисы доступны:
# ToDo     →  http://localhost:8000/docs
# ShortURL →  http://localhost:8001/docs
```
```bash
# Остановить сервисы:
docker-compose down
```
```bash
# или полностью очистить данные:
docker-compose down -v
```

## 🐳 Запуск через Docker (без docker-compose)

### Создание именованных томов
```bash
docker volume create todo_data
docker volume create shorturl_data
```

### Сборка Docker образов
```bash
docker build -t bsergey/todo-service:latest ./todo_app
docker build -t bsergey/shorturl-service:latest ./shorturl_app
```

### Запуск контейнеров
```bash
docker run -d -p 8000:80 -v todo_data:/app/data --name todo-service bsergey/todo-service:latest
docker run -d -p 8001:80 -v shorturl_data:/app/data --name shorturl-service bsergey/shorturl-service:latest
```

## 🌐 Образы на Docker Hub
ToDo Service:
- 📦 `bsergey/todo-service:latest`
- 🔗 https://hub.docker.com/r/bsergey/todo-service

URL Shortener:
- 📦 `bsergey/shorturl-service:latest`
- 🔗 https://hub.docker.com/r/bsergey/shorturl-service

### Запуск готовых образов
```bash
docker run -d -p 8000:80 -v todo_data:/app/data bsergey/todo-service:latest
docker run -d -p 8001:80 -v shorturl_data:/app/data bsergey/shorturl-service:latest
```

## 🧪 Тестирование

### Через браузер (рекомендуется)
ToDo API документация: http://localhost:8000/docs

ShortURL API документация: http://localhost:8001/docs

### Примеры запросов через curl
ToDo Service:

Создание задачи:

```bash
curl -X POST "http://localhost:8000/items" \
  -H "Content-Type: application/json" \
  -d '{"title": "Купить продукты", "description": "Молоко, хлеб, яйца"}'
```

Получение всех задач:

```bash
curl "http://localhost:8000/items"
```

Получение задачи по ID:

```bash
curl "http://localhost:8000/items/1"
```

Обновление задачи:

```bash
curl -X PUT "http://localhost:8000/items/1" \
  -H "Content-Type: application/json" \
  -d '{"completed": true}'
```

Удаление задачи:

```bash
curl -X DELETE "http://localhost:8000/items/1"
```

ShortURL Service:

Сокращение ссылки:

```bash
curl -X POST "http://localhost:8001/shorten" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://github.com"}'
```

Переход по короткой ссылке (в браузере):

```
http://localhost:8001/{short_id}
```

Получение статистики:

```bash
curl "http://localhost:8001/stats/{short_id}"
```

## Проверка сохранения данных
Создайте несколько задач и коротких ссылок

Перезапустите контейнеры:

```bash
docker restart todo-service shorturl-service
```

Проверьте, что данные сохранились:

```bash
curl "http://localhost:8000/items"
curl "http://localhost:8001/stats/{short_id}"
```

## 📋 Чек-лист выполнения задания

Ниже — аккуратно оформленный чек-лист в формате GitHub Task List. Отмеченные пункты выполнены.

- [x] Реализованы ToDo-сервис и сервис сокращения ссылок на FastAPI
- [x] Подключён SQLite для хранения данных (файлы БД монтируются в тома)
- [x] Написан Dockerfile для каждого сервиса
- [x] Использованы тома для сохранения данных (валидация сохранности после рестарта)
- [x] Проведено тестирование локально (curl и Swagger UI /docs)
- [x] Исходный код загружен на GitHub
- [x] В отчёте приведены ссылки на GitHub-репозиторий и образы на Docker Hub

Прогресс: **7 / 7** — все основные пункты выполнены ✅

Полезные ссылки

- GitHub репозиторий: https://github.com/BSergey2000/fastapi-microservices
- ToDo Service на Docker Hub: https://hub.docker.com/r/bsergey/todo-service
- ShortURL Service на Docker Hub: https://hub.docker.com/r/bsergey/shorturl-service

## 📄 Лицензия
Проект распространяется по лицензии MIT.

Автор: BSergey2000